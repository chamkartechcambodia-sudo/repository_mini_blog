# Add project specific ProGuard rules here.
#
# Minification is disabled for this teaching app (see build.gradle.kts), so no keep
# rules are required. If you later enable R8/minify for a release build, you may need
# keep rules for Moshi reflection models and Retrofit interfaces.
